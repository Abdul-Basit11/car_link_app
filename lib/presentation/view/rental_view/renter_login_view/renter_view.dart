import 'package:flutter/material.dart';

import 'layout/body.dart';

class RenterLoginView extends StatelessWidget {
  const RenterLoginView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return RenterLoginViewBody();
  }
}
